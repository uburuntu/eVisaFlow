services:
  bot:
    build:
      context: ..
      dockerfile: service/Dockerfile
    env_file: .env
    volumes:
      - downloads:/app/service/downloads
    restart: unless-stopped

volumes:
  downloads:
